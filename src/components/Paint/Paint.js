import React from 'react';

function Paint(props) {
    React.useEffect(() => {

    }, []);

    return (
        <div>
            <canvas id="paint" />
        </div>
    );
}

export default Paint;